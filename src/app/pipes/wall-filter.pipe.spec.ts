import { WallFilterPipe } from './wall-filter.pipe';

describe('WallFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WallFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
